const imageDefaultClasses = "object-contain";

const imagePrimaryClasses = "h-56 w-auto";

const imageTileLogoClasses =
	"float-left w-36 h-auto mr-5 mt-1 mb-1 border-2  border-solid border-black rounded-3xl p-4";

const imageIconClasses = "absolute w-9 h-auto mr-4 right-0";

const imageExamClasses = "w-full";

const imageTheoryClasses= "min-h-[20rem] w-auto";

export {
	imageDefaultClasses,
	imagePrimaryClasses,
	imageTileLogoClasses,
	imageIconClasses,
	imageExamClasses,
	imageTheoryClasses,
};
